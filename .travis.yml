language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t samnrogers/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true samnrogers/docker-react npm run test -- --coverage